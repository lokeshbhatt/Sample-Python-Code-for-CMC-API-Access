{
   "Parameters": {
      "BaseURL": "https://<base UR as listed on Base URL and API Key Authentication page on CMC>",
      "X-CMC-Client-Secret": "<Client secret as listed on Base URL and API Key Authentication page on CMC>",
      "X-CMC-Client-Id": "<Client id as listed on Base URL and API Key Authentication page on CMC>"
   }
}
